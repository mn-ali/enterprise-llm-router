# =============================================================================
# LiteLLM Proxy Configuration
# =============================================================================
# Docs: https://docs.litellm.ai/docs/proxy/configs
#
# Environment variables referenced below (e.g. os.environ/AZURE_API_KEY)
# are resolved at runtime from the container environment / .env file.
# =============================================================================

# ---------------------------------------------------------------------------
# General Settings
# ---------------------------------------------------------------------------
general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY       # proxy-wide auth key
  database_url: os.environ/DATABASE_URL            # PostgreSQL for spend tracking

litellm_settings:
  drop_params: true          # silently drop unsupported params per-provider
  set_verbose: false         # set true for debug logging
  num_retries: 3             # auto-retry on transient failures
  request_timeout: 600       # seconds

# ---------------------------------------------------------------------------
# Model List
# ---------------------------------------------------------------------------
# Each entry defines a "model_name" (the alias your apps use) and the
# underlying provider + credentials in "litellm_params".
#
# Clients call the API using "model_name" — LiteLLM routes to the real
# provider transparently.
# ---------------------------------------------------------------------------
model_list:

  # =========================================================================
  # Navy AI (OpenAI Compatible) — Primary Provider
  # =========================================================================
  - model_name: navy-gpt-4o
    litellm_params:
      model: openai/gpt-4o
      api_base: https://api.navy/v1
      api_key: os.environ/NAVY_API_KEY

  # =========================================================================
  # Additional Providers (uncomment and configure as needed)
  # =========================================================================

  # --- Azure OpenAI ---
  # - model_name: gpt-4
  #   litellm_params:
  #     model: azure/gpt-4
  #     api_base: os.environ/AZURE_API_BASE
  #     api_key: os.environ/AZURE_API_KEY
  #     api_version: "2024-06-01"

  # - model_name: gpt-4o
  #   litellm_params:
  #     model: azure/gpt-4o
  #     api_base: os.environ/AZURE_API_BASE
  #     api_key: os.environ/AZURE_API_KEY
  #     api_version: "2024-06-01"

  # --- Anthropic (Claude) ---
  # - model_name: claude-sonnet
  #   litellm_params:
  #     model: anthropic/claude-sonnet-4-20250514
  #     api_key: os.environ/ANTHROPIC_API_KEY

  # - model_name: claude-haiku
  #   litellm_params:
  #     model: anthropic/claude-3-5-haiku-20241022
  #     api_key: os.environ/ANTHROPIC_API_KEY

  # --- OpenAI (direct) ---
  # - model_name: openai-gpt-4
  #   litellm_params:
  #     model: openai/gpt-4
  #     api_key: os.environ/OPENAI_API_KEY

  # --- Huggingface ---
  # - model_name: hf-mistral
  #   litellm_params:
  #     model: huggingface/mistralai/Mistral-7B-Instruct-v0.2
  #     api_key: os.environ/HUGGINGFACE_API_KEY

# ---------------------------------------------------------------------------
# Router Settings (load balancing)
# ---------------------------------------------------------------------------
router_settings:
  routing_strategy: least-busy
  num_retries: 3
  timeout: 600
  # Redis (uncomment if using distributed rate limiting):
  # redis_host: os.environ/REDIS_HOST
  # redis_port: os.environ/REDIS_PORT
  # redis_password: os.environ/REDIS_PASSWORD

# ---------------------------------------------------------------------------
# Environment / UI Settings
# ---------------------------------------------------------------------------
environment_variables:
  DOCS_URL: "/docs"
  ROOT_REDIRECT_URL: "/ui"
